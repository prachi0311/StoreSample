<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
<style name="Custom_AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    <item name="colorPrimary">@color/darkgrey</item>
    <item name="colorPrimaryDark">@color/black</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="android:actionMenuTextColor">@color/white</item>
</style>
    <style name="EditTextBoxGrey" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/place_experience_edit_text_background</item>
        <item name="background">@drawable/place_experience_edit_text_background</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:minHeight">100dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:maxHeight">250dp</item>
        <item name="android:gravity">start</item>
    </style>


</resources>
